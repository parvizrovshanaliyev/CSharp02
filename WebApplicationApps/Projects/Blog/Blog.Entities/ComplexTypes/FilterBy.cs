namespace Blog.Entities.ComplexTypes
{
    public enum FilterBy
    {
        Category,
        Date,
        ViewCount,
        CommentCount,
    }

    public enum OrderBy
    {
        Date,
        ViewCount,
        CommentCount,
    }
}
